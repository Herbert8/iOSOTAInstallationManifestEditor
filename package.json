{
  "name": "make-plist",
  "version": "1.0.0",
  "description": "Generate a plist file.",
  "main": "webpack.config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-dev": "webpack; rm dist/*.woff*; rm dist/*.eot; rm dist/*.svg; rm dist/*.ttf",
    "build": "webpack -p; rm dist/*.woff*; rm dist/*.eot; rm dist/*.svg; rm dist/*.ttf"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-webpack-plugin": "^0.1.16",
    "css-loader": "^0.28.4",
    "file-loader": "^0.11.2",
    "html-webpack-plugin": "^2.29.0",
    "style-loader": "^0.18.2",
    "url-loader": "^0.5.9",
    "webpack": "^3.0.0",
    "webpack-clean-obsolete-chunks": "^0.1.10"
  }
}